---
name: st
description: Show task details. Use when the user says "/st", "show task", or asks about a specific task.
disable-model-invocation: true
---

Show task details using the `show_task` MCP tool. Ask the user for the task ID. Report all available fields: title, description, status, priority, assignee, labels, dependencies, and notes.
