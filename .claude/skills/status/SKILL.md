---
name: status
description: Show agent-messenger status overview. Use when the user says "/status" or asks about message counts, active agents, or channel state.
disable-model-invocation: true
---

Run `agent-messenger status` in the shell and show the output to the user. This displays unread message counts per agent, recent messages, active agents, and channels at a glance.
